#include <stdio.h>
int main()
// Seja vet um vetor de 4 elementos: TIPO vet[4]. Supor que depois da declaração, 
// vet esteja armazenado no endereço de memória 4092 (ou seja, o endereço de vet[0]).
// Supor também que na máquina usada uma variável do tipo char ocupa 1 byte, do tipo int ocupa
// 4 bytes, do tipo float ocupa 4 bytes e do tipo double ocupa 8 bytes.
// Qual o valor de vet+1, vet+2 e vet+3 se:
// a)      vet for declarado como char?
// b)      vet for declarado como int?
// c)      vet for declarado como float?
// d) vet for declarado como double

{
	
	return 0;
}