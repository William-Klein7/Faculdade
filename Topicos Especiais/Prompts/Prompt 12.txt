Agora refatore o cálculo para usar uma lista de regras de desconto.

Exemplo:
const discountRules = [
  { code: "URI10", type: "percentage", value: 10 },
  { code: "FRETEGRATIS", type: "shipping", value: 40 }
];

A função calculateCartSummary deve aplicar a regra correta com base no cupom.

Deixe o código genérico para no futuro aceitar novos cupons.
Atualize tudo que for necessário.